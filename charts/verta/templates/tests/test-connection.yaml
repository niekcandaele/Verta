apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "verta.fullname" . }}-test-connection"
  labels:
    {{- include "verta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "verta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
